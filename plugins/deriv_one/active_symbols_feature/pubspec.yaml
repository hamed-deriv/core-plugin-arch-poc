name: active_symbols_feature
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

  active_symbols_deriv_one:
    git:
      url: git@github.com:naif-deriv/core-plugin-arch-poc.git
      ref: master
      path: core/active_symbols/adapters/active_symbols_deriv_one

  bloc: ^8.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
